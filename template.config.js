module.exports = {
    // Placeholder used to rename and replace in files
    // package.json, index.json, android/, ios/
    placeholderName: 'react_native_template',

    // Directory with template
    templateDir: './template',
}
